spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowe_db
    username: postgres
    password: rx7veilside
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: always

server:
  port: 8080